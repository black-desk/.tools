if grep -q Microsoft /proc/version; then
  
  $my_tools_dir/clash-bin/clash-amd64

else
  
  if grep -q mokee /proc/version; then
     
    $my_tools_dir/clash-bin/clash-arm

  else 
    
    if grep -q centos /proc/version; then
  
      $my_tools_dir/clash-bin/clash-amd64

    else 
      
      $my_tools_dir/clash-bin/clash-amd64

    fi

  fi

fi
